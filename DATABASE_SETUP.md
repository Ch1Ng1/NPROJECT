# 🎯 Инструкция за Настройка на MySQL База Данни

## Вариант 1️⃣: През phpMyAdmin (Препоръчително)

### Стъпки:
1. **Отворете phpMyAdmin**: http://localhost/phpmyadmin
2. **Кликнете на "SQL"** в горния лявата меню
3. **Копирайте и залепете съдържанието на `database.sql`**
4. **Кликнете "Go" или натиснете Ctrl+Enter**
5. ✅ Базата се създаде автоматично със всички таблици!

---

## Вариант 2️⃣: През MySQL Командна Линия

```bash
# 1. Свържете се към MySQL
mysql -u root -p

# 2. Във вътрешния MySQL интерпретатор:
# - Ако нямате парола, просто натиснете Enter
# - Или използвайте: mysql -u root (без -p)

# 3. Импортирайте SQL файла
SOURCE "c:\xampp\htdocs\NPROJECT\database.sql";

# 4. Проверете дали е създадена базата
SHOW DATABASES;

# 5. Изберете я
USE football_predictor;

# 6. Вижте таблиците
SHOW TABLES;
```

---

## Вариант 3️⃣: През Python (Автоматично)

Ще имплементирам скрипт за инициализация на базата. Той ще:
- Прочете `database.sql`
- Създаде базата автоматично от Python

---

## 📋 Какво Съдържат Таблиците?

| Таблица | Назначение |
|---------|-----------|
| `teams` | Всички отбори с API ID, име, лига |
| `matches` | Мачове с резултати, статус, дата |
| `team_statistics` | Статистика на отборите (последни 5 мача) |
| `predictions` | Всички направени прогнози |
| `elo_history` | История на ELO рейтингите |
| `h2h_statistics` | Head-to-Head статистика между отбори |
| `prediction_accuracy` | Точност на прогнозите |

---

## 🔧 Конфигурация в .env

Фајла `.env` вече имам обновен с:
```
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=          ← ОСТАВЕТЕ ПРАЗНО ако нямате парола
DB_NAME=football_predictor
DB_PORT=3306
```

**Ако имате парола на MySQL**, добавете я:
```
DB_PASSWORD=your_mysql_password
```

---

## ✅ Проверка на Свързването

След като импортирате базата, стартирайте Python скрипт за проверка:

```python
from database import DatabaseManager

db = DatabaseManager()
if db.connection and db.connection.is_connected():
    print("✅ Успешно свързване към базата!")
    # Проверите броя таблици
    cursor = db.connection.cursor()
    cursor.execute("SHOW TABLES")
    tables = cursor.fetchall()
    print(f"📊 Таблици: {len(tables)}")
else:
    print("❌ Грешка при свързване")
```

---

## 📝 Следващи Стъпки

1. ✅ Създайте базата чрез един от вариантите
2. ⬜ Инсталирайте `mysql-connector-python` пакета
3. ⬜ Интегрирайте базата в `app.py`
4. ⬜ Добавете логика за съхранение на статистиката

Кажете ми когато завършите базата и ще ви помогна с интеграцията! 🚀
