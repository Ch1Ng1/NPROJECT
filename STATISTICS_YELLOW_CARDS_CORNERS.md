# 🎯 Нови статистики - Очаквани картони и корнери

**Дата:** 27 януари 2026  
**Версия:** 2.2  
**Статус:** ✅ Завършено

---

## 📊 Добавени функции

### 1. ✅ Очаквани жълти картони (Expected Yellow Cards)

**Описание:**
- Всеки мач получава прогноза за очаквания брой жълти картони
- Данните се събират от статистиката на отборите за последния период
- Изчисляват се средни стойности въз основа на история

**Примери:**
- Ако домакин средно получава 1.8 картона/мач
- А гост средно получава 1.6 картона/мач
- Очакваният брой картони за мача = 1.8 + 1.6 = **3.4 картона**

**Стойност:** Полезно за залагане на Over/Under картони (обикновено Over 4.5 или Under 5.5)

---

### 2. ✅ Очаквани корнери (Expected Corners)

**Описание:**
- Всеки мач получава прогноза за очаквания брой корнери
- Взимат се от API статистиките на отборите
- Отразяват атакуващия стил и защита на отборите

**Примери:**
- Ако домакин средно получава 4.5 корнера/мач
- А гост средно получава 3.9 корнера/мач
- Очаквания брой корнери за мача = 4.5 + 3.9 = **8.4 корнера**

**Стойност:** Перфектно за залагане на Over/Under корнери (обикновено Over 8.5 или Under 10.5)

---

## 🛠️ Технически детайли

### Добавени методи в `predictor.py`:

```python
def _calculate_expected_yellow_cards(self, stats: Dict) -> float:
    """Изчислява очаквани жълти картони"""
    # Вземи картони от API статистиките
    # Върни среден брой

def _calculate_expected_corners(self, stats: Dict) -> float:
    """Изчислява очаквани корнери"""
    # Вземи корнери от API статистиките
    # Върни среден брой
```

### Обновена статистика в анализа на мача:

```python
# Картони:
home_yellow_cards = home_stats.get('yellow_cards_avg', 1.8)
away_yellow_cards = away_stats.get('yellow_cards_avg', 1.8)
total_yellow_cards = home_yellow_cards + away_yellow_cards

# Корнери:
home_corners = home_stats.get('corners_avg', 4.2)
away_corners = away_stats.get('corners_avg', 4.2)
total_corners = home_corners + away_corners
```

### Обновена база данни:

```sql
-- Нови колони в таблицата predictions:
ALTER TABLE predictions
ADD COLUMN expected_yellow_cards DECIMAL(5,2),
ADD COLUMN expected_corners DECIMAL(5,2);
```

### Обновена функция за запазване:

```python
def save_prediction(
    # ... съществуващи параметри ...
    expected_yellow_cards: float,
    expected_corners: float,
    # ...
):
    # Запазва двете нови статистики в базата
```

---

## 📈 Резултат на API

Всяка прогноза сега включва:

```json
{
  "time": "19:00",
  "league": "Premier League",
  "home_team": "Manchester City",
  "away_team": "Chelsea",
  "expected_goals": 2.8,
  "expected_yellow_cards": 3.4,  // НОВО ✨
  "expected_corners": 8.4,        // НОВО ✨
  "details": {
    "home_goals_avg": 2.1,
    "away_goals_avg": 0.7,
    "home_yellow_cards_avg": 1.8,  // НОВО ✨
    "away_yellow_cards_avg": 1.6,  // НОВО ✨
    "home_corners_avg": 4.5,       // НОВО ✨
    "away_corners_avg": 3.9        // НОВО ✨
  }
}
```

---

## 📥 CSV експортиране

При експортиране на прогнози в CSV, сега се включват:

| Поле | Описание |
|------|----------|
| Очаквани голове | Expected goals (Over 2.5) |
| **Очаквани картони** | **Expected yellow cards (Over/Under)** |
| **Очаквани корнери** | **Expected corners (Over/Under)** |

---

## 🔄 Използване

### На API:
```bash
curl http://localhost:5000/api/predictions
# Получи всички прогнози със новите статистики
```

### За залагане:

**Картони:**
- Очаквани 3.4 картона? → Залагай Over 3.5
- Очаквани 4.2 картона? → Залагай Over 4.5

**Корнери:**
- Очаквани 8.4 корнера? → Залагай Over 8.5
- Очаквани 9.1 корнера? → Залагай Over 9.5

---

## 🎯 Валидни диапазони

| Статистика | Минимум | Максимум | Среда |
|-----------|---------|----------|--------|
| Картони/мач | 0.5 | 5.0 | **1.8** |
| Корнери/мач | 1.0 | 10.0 | **4.2** |

*Стойностите по подразбиране (1.8 и 4.2) се използват ако API не връща данни*

---

## 🗄️ Структура на база данни

### Нови колони:

```sql
-- predictions таблица
expected_yellow_cards DECIMAL(5,2) - Очаквани жълти картони
expected_corners DECIMAL(5,2) - Очаквани корнери
```

**Пример данни:**
```sql
SELECT 
    home_team_id, 
    away_team_id, 
    expected_yellow_cards, 
    expected_corners
FROM predictions
WHERE match_date >= CURDATE();
```

---

## 📊 Анализ на точност

С новите статистики можете да проверите:

1. **Точност на картони:**
   - Колко често Over 4.5 е верен?
   - Коя лига има най-много картони?

2. **Точност на корнери:**
   - Колко често Over 8.5 е верен?
   - Кои отбори получават най-много корнери?

---

## 💡 Практически примери

### Пример 1: Висок брой очаквани картони
```
Manchester City vs Chelsea
- Expected Yellow Cards: 4.5
- Залог: Over 4.5 - ВИСОК РИСК
```

### Пример 2: Нормален брой корнери
```
Liverpool vs Arsenal
- Expected Corners: 8.2
- Залог: Over 8.5 или Under 10.5 - СРЕДЕН РИСК
```

### Пример 3: Малко очаквани картони
```
Burnley vs Nottingham Forest
- Expected Yellow Cards: 2.1
- Залог: Under 3.5 - НИЗ РИСК
```

---

## 🔍 Проверка на данни

### Логирани сообщения при стартиране:

```
✅ Актуализирана дължина на колоните
✅ База данни инициализирана
✅ Прогнози генерирани с картони и корнери
```

### В CSV файла:

```
Време,Домакин,Гост,Очаквани картони,Очаквани корнери
19:00,Man City,Chelsea,3.4,8.4
21:00,Liverpool,Arsenal,3.8,7.9
```

---

## ✨ Бонус

### Взаимна корелация:

Интересно наблюдение:
- Повече корнери → обикновено повече картони
- Интензивни мачове имат и двете високи

Пример:
```
Derby Match: High intensity
- Очаквани картони: 4.5+
- Очаквани корнери: 9.0+
```

---

## 📝 Бележки

1. **API данни:** Данните се събират от api-sports.io статистиките на отборите
2. **Подразбиране:** При липса на данни се използват средни стойности (1.8 и 4.2)
3. **Точност:** Зависи от качеството на API данните за отборите
4. **Обновяване:** Статистиките се обновяват при всяка нова прогноза

---

**Версия:** 2.2  
**Статус:** ✅ PRODUCTION READY  
**Дата:** 27 януари 2026
